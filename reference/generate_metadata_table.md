# Generate Metadata Table

This function parses through the data/scores folders and generates the
metadata table using the metadata.jsons for each assignment.

## Usage

``` r
generate_metadata_table(dir = ".")
```

## Arguments

- dir:

  specify directory

## Value

a dataframe
